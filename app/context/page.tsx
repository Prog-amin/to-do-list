import { Layout } from '@/components/layout/Layout';
import { ContextContent } from '@/components/pages/ContextContent';

export default function ContextPage() {
  return (
    <Layout>
      <ContextContent />
    </Layout>
  );
}
